name: Publish (Manual)

# Dispatched by the Release workflow after a bump
on:
  workflow_dispatch:
    inputs:
      packages:
        description: "Space-separated package names to publish (e.g. @versu/core @versu/cli)"
        type: string
        required: true

jobs:
  publish:
    name: Publish
    runs-on: ubuntu-latest

    permissions:
      id-token: write  # Required for OIDC
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          registry-url: "https://registry.npmjs.org"
          package-manager-cache: false 

      - name: Install dependencies
        run: npm ci

      - name: Build all packages
        run: npm run build

      - name: Test all packages
        run: npm test

      - name: Publish workspaces
        env:
          PACKAGES: ${{ inputs.packages }}
        run: |
          for package in $PACKAGES; do
            npm publish --workspace "$package" --access public
          done
